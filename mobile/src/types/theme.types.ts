export type ThemeColors = {
  primary: string;
  secondary: string;
  background: string;
  card: string;
  foreground: string;
};

export type ThemeScheme = {
  light: ThemeColors;
  dark: ThemeColors;
};
